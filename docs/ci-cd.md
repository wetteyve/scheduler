# CI /CD

GitHub Actions automatically builds and tests the project on every commit and pull request using the following matrix:

* **Node.js**: `20`, `22`
* **OS**: macOS, Linux, Windows

This ensures the native addon remains stable and compatible across platforms.
